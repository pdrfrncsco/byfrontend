import { TransferDetailPage } from './TransferDetailPage'

/** Club-scoped detail (same component; scope inferred from path). */
export default function ClubTransferDetailPage() {
  return <TransferDetailPage scope="club" />
}
